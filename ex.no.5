#include <stdio.h>

int main(){
    FILE *fp=fopen("test.txt","w");
    fprintf(fp,"Hello OS");
    fclose(fp);

    char str[20];
    fp=fopen("test.txt","r");
    fscanf(fp,"%s",str);
    printf("%s\n",str);
    fclose(fp);
    return 0;
}
